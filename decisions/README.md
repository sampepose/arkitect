# Decisions

One file per decision among code-legal options, D-nnn.md, written and read by
`python3 -m arkitect.harness.decisions` (see `arkitect/harness/decisions.py` for the format). A project's
decisions are its own; the engine ships with none.
