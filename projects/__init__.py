"""One directory per property. A project owns its model, its sheets, its build script,
its deliverables and its tests, and imports arkitect/lib/ and arkitect/codes/. It never imports another
project."""
