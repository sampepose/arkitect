"""300 S Elm's own tests: its model, its sheets, its figures.

The engine's tests are in lib/verify/ and the code layer's in codes/verify/. A test
belongs with the thing it constrains, or two projects end up sharing one directory of
assertions about one of them."""
